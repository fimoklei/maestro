Tiny sync-state dot placed before a target name in sidebars and lists — the quietest form of the ok/drift signal.

```jsx
<StatusDot status="ok" /> Claude Code
<StatusDot status="drift" /> ~/dev/acme-web
```

Always pair with a label; never use alone as the only status indicator on a card (use Chip there).
