Uppercase mono tag identifying a primitive's type; the four type colors are fixed vocabulary across the whole product (skill = blue, hook = purple, mcp = teal, bundle = amber).

```jsx
<TypeTag type="skill" />
<TypeTag type="bundle" />
```

Never invent new type colors; never use these four colors for anything other than primitive types.
