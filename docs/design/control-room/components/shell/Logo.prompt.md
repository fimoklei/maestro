The Maestro brand mark — an amber tile with a bold mono "M". Lives at the far left of the status bar.

```jsx
<Logo size={26} wordmark context="agent-harness · main · 9 primitives" />
<Logo size={48} />
```

The tile is always amber on near-black; never recolor it, never add effects.
