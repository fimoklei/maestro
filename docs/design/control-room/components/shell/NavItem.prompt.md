Sidebar navigation item; icons are unicode glyphs, never SVG icon sets.

```jsx
<NavItem icon="▤" label="Inventory" active />
<NavItem icon="⇶" label="Deploy-state" />
<NavItem icon="⧉" label="Compose" />
```

The active state raises the surface and turns the glyph amber — the label never changes color to amber.
