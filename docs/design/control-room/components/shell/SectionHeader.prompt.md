Section title row for main-column sections; the meta slot carries a dim mono annotation with `·` separators.

```jsx
<SectionHeader title="Central inventory" meta="curated · production-ready">
  <Button variant="primary">+ new primitive</Button>
</SectionHeader>
```

Titles are short noun phrases in sentence case; meta is lowercase mono.
