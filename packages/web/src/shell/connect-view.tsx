// Placeholder for the connect/settings view (slice 4c). The route exists now so
// the sidebar nav is coherent; the offline-connect flow is wired in its own
// slice. Kept minimal on purpose — no behaviour to test yet.
export function ConnectView() {
  return (
    <section>
      <h2>Connect</h2>
      <p>Connect your central inventory. Coming in a later slice.</p>
    </section>
  );
}
